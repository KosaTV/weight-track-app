const setApplicationSize = () => {
	// if (navigator.userAgent.includes("Chrome")) document.documentElement.style.setProperty("--system-height", `${window.innerHeight}px`);
};

export {setApplicationSize};
